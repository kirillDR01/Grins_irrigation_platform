{
  "name": "Type Check on Save",
  "version": "1.0.0",
  "description": "Runs mypy type checking on Python files when edited",
  "disabled": true,
  "when": {
    "type": "fileEdited",
    "patterns": ["src/**/*.py"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Run mypy on the edited file if needed."
  }
}
