# Cluster A — Evidence Dossier

Each row: task ID, status (PASS / FAIL / BLOCKED), evidence file paths, optional reviewer notes.
Final task verifies every row is PASS.

| Task | Status | Evidence | Notes |
|---|---|---|---|
| T1.0 — Bootstrap evidence dir | PASS | `phase-1-foundation/T1.0-bootstrap.txt` | 7 phase subdirs + dossier created |
| T1.1 — Create S3UploadError | PASS | `phase-1-foundation/T1.1-exception-class.txt` | retryable flag works; re-export from `exceptions/__init__.py` confirmed |
| T1.2 — Wrap PhotoService.put_object | PASS | `phase-1-foundation/T1.2-ruff.txt`, `T1.2-mypy.txt`, `T1.2-photo-service-existing-tests.txt`, `T1.2-diff.txt` | ruff clean, mypy clean, 8/8 disposition tests pass |
| T1.3 — Map S3UploadError → 502/503 in leads.py | PASS | `phase-1-foundation/T1.3-lead-attachment-tests.txt`, `T1.3-diff.txt` | 2/2 lead_attachment tests pass; other lead test failures (UTM mocks) are pre-existing, unrelated |
| T1.4 — Map S3UploadError → 502/503 in customers.py | PASS | `phase-1-foundation/T1.4-customer-photo-tests.txt`, `T1.4-diff.txt` | 2/2 customer_photo/document tests pass; both upload paths (photo + document) covered |
| T1.5 — Backfill customer.internal_notes | PASS | `phase-1-foundation/T1.5-alembic-upgrade.txt`, `T1.5-customer-notes-after-backfill.sql.txt`, `T1.5-roundtrip.txt`, `T1.5-idempotency-diff.txt`, `T1.5-sample-merged.sql.txt` | 108 customers, 15 source rows merged, 0 truncated; idempotency_diff_lines=0; rechain off 20260511_120000 (plan's stated head was stale); dropped appointment.customer_id branch (column doesn't exist — only job.customer_id reachable); schemas/customer.py bumped to max_length=50_000 |
| T1.6 — Drop appointment_notes table | PASS | `phase-1-foundation/T1.6-alembic-upgrade.txt`, `T1.6-appointment-notes-table-state.sql.txt`, `T1.6-roundtrip.txt` | 0 non-empty rows discarded; table dropped + restored cleanly via downgrade |
| T1.7 — Migrate appointment_attachments → customer_photos | PASS | `phase-1-foundation/T1.7-before-count.sql.txt`, `T1.7-alembic-upgrade.txt`, `T1.7-attachments-dropped.sql.txt`, `T1.7-after-photo-count.sql.txt`, `T1.7-roundtrip.txt` | Before=0 / After=0 (dev DB is empty post-wipe); orphan path covered; round-trip clean. appointments has no direct customer_id — only path is appointments.job_id → jobs.customer_id |
| T1.8 — Verify alembic chain | PASS | `phase-1-foundation/T1.8-alembic-current.txt`, `T1.8-alembic-heads.txt`, `T1.8-alembic-history.txt` | Single head `20260513_120200`; linear chain through 20260513_120100 → 20260513_120000 → 20260511_120000 → 20260512_120000 → 20260510_120000 |
| T1.9 — Verify app boot | PASS | `phase-1-foundation/T1.9-app-boot.txt` | create_app() succeeds, 370 routes registered. Note: appointment_note + appointment_attachment models/services/endpoints still exist in source — boot doesn't query them. They're slated for Phase 6 deletion. |
| T2.1 — Add _carry_forward_lead_attachments | PASS | `phase-2-service-layer/T2.4-diff.txt`, `T2.4-lead-service-tests.txt` | Idempotent on file_key; audit-logs `lead.cascade.attachments_moved` when count>0; ruff/mypy delta clean |
| T2.2 — Add _cascade_lead_intake_tag | PASS | `phase-2-service-layer/T2.4-diff.txt`, `T2.4-lead-service-tests.txt` | Uses CustomerTagRepository.get_by_customer_and_label pre-check (avoids IntegrityError rollback); humanize_tag truncates to 32 chars |
| T2.3 — Add _cascade_lead_action_tags | PASS | `phase-2-service-layer/T2.4-diff.txt`, `T2.4-lead-service-tests.txt` | Filters ESTIMATE_APPROVED + ESTIMATE_REJECTED via TERMINAL_ACTION_TAGS; same pre-check pattern as intake_tag |
| T2.4 — Umbrella _carry_forward_lead_data + 3 call sites | PASS | `phase-2-service-layer/T2.4-diff.txt`, `T2.4-lead-service-tests.txt` (65 passed) | Plan said convert_lead had a 3rd call site — it did NOT. Cascade was added to convert_lead anyway per spec intent ("every conversion entry point cascades"). _carry_forward_lead_notes now returns bool. Test fixtures across 6 lead test files updated to neutralize session.execute chain so cascade helpers act as no-ops in mock-only tests. |
| T2.5 — AppointmentService overwrite customer.internal_notes | PASS | `phase-2-service-layer/T2.5-appointment-service-diff.txt`, `T2.5-appointment-notes-tests.txt` (7 passed) | Removed `[timestamp] Appointment note: ` prefix → bare overwrite. `appointment.notes` legacy column still dual-written through Phase 5. Existing PBT renamed `test_add_notes_appends_to_customer_internal_notes` → `test_add_notes_overwrites_customer_internal_notes`; functional happy-path test updated to assert equality not substring. |
| T3.1 — JobResponse.customer_tags field | PASS | `phase-3-api/T3-phase-3-diff.txt` | Added `customer_tags: list[CustomerTagResponse] \| None` to `schemas/job.py`. |
| T3.2 — JobResponse builder populates customer_tags | PASS | `phase-3-api/T3-phase-3-diff.txt` | Added `_populate_customer_tags(job, resp)` helper in `api/v1/jobs.py`; wired into list_jobs (line 452), ready-to-schedule (507), get_job_by_id (721), complete_job (1213). Customer.tags is `lazy="selectin"` so no extra eager-load needed. |
| T3.3 — AppointmentResponse customer_tags | PASS | `phase-3-api/T3-phase-3-diff.txt` | Added field to `schemas/appointment.py`; populated inside `_populate_appointment_extended_fields` so every list/get/weekly/daily endpoint includes it. |
| T3.4 — SalesEntryResponse customer_tags | PASS | `phase-3-api/T3.4-sales-pipeline-tests.txt` (42 passed) | Added field to `schemas/sales_pipeline.py`; populated inside `_entry_to_response`. Test fixtures `_make_entry` + `_make_response_entry_mock` + `_make_customer_mock` updated so Pydantic `from_attributes` doesn't pick up MagicMock proxies for the new field. |
| T4.1 — Install shadcn Command primitive | PASS | `phase-4-frontend/T4.1-frontend-build.txt` (pre-existing TS errors unrelated; cmdk added; command.tsx created) | Pre-existing TS errors in EstimateBuilder/SalesEstimateCreator/nowContent confirmed via git stash + rebuild; not introduced by Phase 4. |
| T4.2 — Create TagPicker component | PASS | `frontend/src/features/customers/components/TagPicker.tsx` | Self-saving via useSaveCustomerTags; combobox with inline create; system tags non-removable; 32-char cap. |
| T4.3 — Replace TagEditorSheet internals | PASS | `TagEditorSheet.tsx` reduced to Sheet wrapper; `TagEditorSheet.test.tsx` rewritten to 3 mock-TagPicker tests (all green) | Old 600-line internals test deleted; coverage shifted to TagPicker.test.tsx. |
| T4.4 — Mount TagPicker on every tag surface | PASS | CustomerDetail (Card section), SalesDetail (Card section), JobDetail (notes-row sibling), AppointmentDetail (slate-rounded section), LeadDetail (post-conversion only) | Lead pre-conversion shows placeholder text per spec. |
| T4.5 — Create CustomerNotesEditor | PASS | `frontend/src/features/customers/components/CustomerNotesEditor.tsx` (auto-save-on-blur + Saved flash + readOnly + invalidateAfterCustomerInternalNotesSave) | 4 tests pass (T5.6 evidence). |
| T4.6 — Rewire notes surfaces | PARTIAL | JobDetail now writes customer.internal_notes via CustomerNotesEditor (was writing job.notes legacy); LeadDetail post-conversion swapped to CustomerNotesEditor. | CustomerDetail/SalesDetail/AppointmentForm InternalNotesCard kept — they already write customer.internal_notes and a full swap would break 77+ existing test-id assertions. CustomerNotesEditor is the new pattern; full migration deferred to Phase 6 cleanup. |
| T4.7 — Deprecate .notes types | PASS | JSDoc @deprecated added to Job.notes, Appointment.notes, SalesEntry.notes. | TypeScript build still clean (only pre-existing errors). |
| T4.8 — Update tag invalidation matrix | PASS | `useCustomerTags.ts` onSettled now invalidates customerKeys.detail/lists, jobKeys.lists(), appointmentKeys.lists(), pipelineKeys.lists(). | Lets denormalized Job/Appointment/Sales rows reflect the new tag set without manual refresh. |
| T5.1 — Cascade unit tests | PASS | `phase-5-tests/T5-all-new-tests.txt` — 7 cascade tests | Inserts/idempotent attachments, intake_tag create + dupe-skip, action_tags terminal-filter + empty no-op, umbrella emits structured complete log. |
| T5.2 — S3 error unit tests | PASS | `phase-5-tests/T5-all-new-tests.txt` — 5 tests | ClientError/EndpointConnectionError/BotoCoreError → retryable=True; NoCredentialsError → retryable=False; happy-path returns UploadResult. Tiny JPEG generated in-memory via Pillow (avoids hex-encoding fragility). |
| T5.3 — API 502/503 mapping tests | PASS | `phase-5-tests/T5-all-new-tests.txt` — 5 tests in `test_upload_api_s3_error_mapping.py` | Customer photos endpoint: retryable→502, misconfig→503, 404 regression. Lead attachments endpoint: 502/503 via PhotoService patch on module path. |
| T5.4 — Integration denormalization tests | PASS | `phase-5-tests/T5-all-new-tests.txt` — 5 tests in `tests/integration/test_cluster_a_denormalization.py` | Job + Appointment customer_tags populated; empty array vs None preserved; unloaded customer leaves field None. |
| T5.5 — Update existing notes tests | PASS | `phase-5-tests/T5.5-existing-suites.txt` — 140 existing tests still green | Dual-write assertions already in place from Phase 2 (`# DEPRECATED_DUAL_WRITE_PHASE_6` comments + parallel `customer.internal_notes` checks); no further changes needed. |
| T5.6 — Frontend component tests | PASS | `phase-5-tests/T5.6-frontend-tests.txt` — 8/8 tests | TagPicker: render chips, empty placeholder, inline-create row, save invocation. CustomerNotesEditor: render initial, no-mutate on unchanged blur, single mutate on change, readOnly disables. |
