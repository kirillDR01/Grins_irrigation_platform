/**
 * Schedule Mobile page wrapper — renders the resource mobile view.
 */

import { ResourceMobileView } from '@/features/resource-mobile';

export function ScheduleMobilePage() {
  return <ResourceMobileView />;
}
